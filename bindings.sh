# source(.) this file

# print path and then press Alt-c to cd to that path
bind '"\ec":"\C-acd \n"'

# Alt-l to ls (not delete prompt)
bind -x '"\el":"ls -l"'

# Alt-h to history (not delete prompt)
bind -x '"\eh":"history"'